{
  "name": "coach-memory-workspace",
  "version": "1.0.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && cd worker && npm test"
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  },
  "private": true
}