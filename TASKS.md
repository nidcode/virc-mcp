# TASKS — コーチングUX 対応事項

2026-09-06 に実施した UXレビュー（隔離テスト用ウィキで `dispatch()` を直接実行し、
主要フローとエラーパスを実測）から起票。各項目に根拠・理由・対応案・完了条件を記す。

## P0 — 新規ユーザーの初回体験が空白

**根拠**: 空のウィキで `get_coach_briefing` を呼ぶと、返るのは
```
禁則:
反応モデル:
```
の2行のみで、直後に1270文字の内部ルール文（PROTOCOL）が続く。目標・基準値・制約が
何も無い状態で「専属コーチとして振る舞え」しか渡らず、選手のプロフィールを聞き出す
導線が無い。現状は `raw/*.md` → 抽出 → `seed.js` という開発者向けワークフローで
人力補完している。

**なぜ問題か**: 「記憶と判断のレイヤー」という製品の売りは、選手のプロフィールが
蓄積されて初めて効いてくる。初回にそれを聞き出す設計が無いと、新規ユーザーは
何も知らない一般的なコーチと区別がつかない状態からスタートする。

- [ ] briefing が空（athlete_profile 無し）のときに、コーチへ「最初に何を聞くべきか」
      （目標レース・現在の練習頻度・故障歴・絶対禁則になりそうな制約）を提示する
      導線を追加する
- [ ] 初回ヒアリングで集まった内容を `record_memory` / `remember` に落とす、
      という手順を PROTOCOL または briefing の空状態メッセージに明記する

**完了条件**: 空のウィキで `get_coach_briefing` を呼んだとき、返り値に
「何を聞き出すべきか」の具体的な指示が含まれる。

## P1 — IDを選手に見せない、という約束にUI層の抜け道がある

**根拠**: `record_decision` の成功メッセージは `decision d_02 / prediction p_02` を、
`record_memory` は `response_tendency を rt_01 として記録しました` をそのまま返す。
⛔ルールは「返答の“文章”にIDを書くな」という、LLMの地の文にしか効かない指示であり、
Claude.ai / ChatGPT 等の「ツール使用」展開パネルで生の引数・戻り値が選手にも
見えるクライアントでは、そのままIDが露出する。

**なぜ問題か**: 「記録IDを選手に見せない」は本プロジェクトが明示的に最重要視している
ルールの一つ（`.claude/skills/coach/`, `schema/CLAUDE.md` に繰り返し明記）。
それが指示層でしか守られておらず、UIの透明性機能で無効化される。

- [ ] 成功メッセージからIDを人間向け文言だけに削る、もしくはIDを別フィールド
      （ツール専用のメタデータ）に分離できないか検討する
      （`remember` が既にID非開示で返している設計を他の書き込み系ツールにも広げる）
- [ ] 少なくとも、利用が想定されるクライアント（Claude.ai / ChatGPT connector）で
      実際にツール呼び出しパネルにIDが見えるかを確認する

**完了条件**: 主要な接続経路で、ツール呼び出しの生の戻り値を選手が見ても
記録IDが読み取れない（または、そのリスクを許容する理由が明文化されている）。

## P2 — `record_outcome` の `refuted` 誤用ガードが説明だけで強制力がない

**根拠**: ツール説明文に「検証できなかった場合に `refuted` を使わない」と明記されて
いるにもかかわらず、`record_outcome(p_01, status: 'refuted', observed: '9/1に実施
できず延期になった')`（本来は `unadjudicable` に該当するケース）が無検証で通った。

**なぜ問題か**: briefing冒頭に毎回表示される「的中率」はこの製品の信頼指標であり、
延期を "外れ" として数えると、この数字が静かに汚染される。

- [ ] `observed` の文言や、審判不能を示す典型パターン（延期・中止・実施できず等）を
      検知して `refuted` 選択時に注意を促す、もしくは拒否する
- [ ] 難しければ最低限、`get_coach_briefing` の「これまでの予測」表示に、
      判定根拠を再確認できる導線（該当予測へのリンク等）を足す

**完了条件**: 「実施できなかった」ことが明確な `observed` を伴う `refuted` 呼び出しが、
警告なしには通らない。

## P3 — `record_decision` の段階的バリデーションが往復コストになっている

**根拠**: 正しい呼び出しに至るまで実測で3回連続拒否（予測欄欠落 → 絶対禁則未確認 →
`constraint_compliance` の文字数不足）。`constraint_compliance` が絶対禁則の有無に
応じて条件付き必須になることは `inputSchema` にも `description` にも事前に書かれて
おらず、モデルは拒否されて初めて要件を知る。

- [x] `record_decision` の tool description に「絶対禁則が存在する場合は
      `constraint_compliance`（20字以上）も必須」と明記する
- [x] 可能なら `get_coach_briefing` の返り値に「現在の絶対禁則一覧」がある以上、
      それをそのまま `constraints_reviewed` の期待値として案内する一文を添える

**対応**: `core/tools.ts` の `record_decision` ツール定義（`TOOLS` 配列）を編集。
トップレベル description に条件付き必須の一文を追加し、`constraint_compliance` の
description にも「absolute 禁則が1件でも有効に存在する場合は必須」と明記。
`constraints_reviewed` の description には briefing の「禁則:」欄の `[!]` 付き id を
そのまま使えばよい旨を追記した（バリデーションロジック自体は変更していない）。

**完了条件**: 絶対禁則がある状態での初回 `record_decision` 呼び出しの成功率が
実測で上がる（現状: 1回目成功0/1）。

## P4 — briefing 固定文の再送コスト（低優先・監視事項）

**根拠**: `PROTOCOL` は1270文字あり、`get_coach_briefing` の返り値の6割以上を占める。
MCPにシステムプロンプト注入経路が無いための意図的な設計（コード内コメントに明記済み）
だが、セッション開始のたびに・30分TTL切れのたびに再送される固定コストである。

- [ ] ルールが増えてきたら、静的な部分と動的な部分（記憶データ）を分離できないか
      再検討する（例: 一部を MCP resources 側に寄せ、briefing 本体を軽くする）

**完了条件**: 特になし（利用実績が増えてから優先度を見直す）。

## 参考（対応済み・運用メモ）

`worker/package.json` の `deploy` スクリプトが `wrangler.jsonc` の実体と食い違い、
`npm run deploy` を実行すると本番ではなく別の空ワーカー（`coach-graph-production`）に
"サイレント成功"していた。2026-09-06 に発見・修正・本番反映・誤ワーカー削除済み
（コミット `366b057`）。exit code 0 がそのままデプロイ成功を意味しない典型例として記録。
